const test = require('ava')
const analyze = require('../../../src/analyzer/selectors/id')

test.todo('it responds with the correct structure')

test.todo('it counts the total of all id selectors')

test.todo('it counts the total of all unique id selectors')

test.todo('it finds all unique id selectors, sorts them and adds a count')

test.todo('it does not report selectors that are not id selectors')
