const test = require('ava')
const analyze = require('../../../src/analyzer/selectors/js')

test.todo('it responds with the correct structure')

test.todo('it counts the total of all js selectors')

test.todo('it counts the total of all unique js selectors')

test.todo('it finds all unique js selectors, sorts them and adds a count')

test.todo('it does not report selectors that are not js selectors')
