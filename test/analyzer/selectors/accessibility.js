const test = require('ava')
const analyze = require('../../../src/analyzer/selectors/accessibility')

test.todo('it responds with the correct structure')

test.todo('it counts the total of all accessibility selectors')

test.todo('it counts the total of all unique accessibility selectors')

test.todo(
  'it finds all unique accessibility selectors, sorts them and adds a count'
)

test.todo('it does not report selectors that are not accessibility selectors')
