const test = require('ava')
const analyze = require('../../../src/analyzer/selectors/universal')

test.todo('it responds with the correct structure')

test.todo('it counts the total of all universal selectors')

test.todo('it counts the total of all unique universal selectors')

test.todo(
  'it finds all unique universal selectors, sorts them and adds a count'
)

test.todo('it does not report selectors that are not universal selectors')
