const test = require('ava')
const analyze = require('../../../src/analyzer/selectors')

test.todo('it responds with the correct structure')

test.todo(
  'it finds the selectors with the most identifiers and it sorts them by count'
)

test.todo('it finds the selector with the most identifiers')

test.todo('it calculates the average amount of identifiers per selector')
